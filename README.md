🧾 Java Receipt Generator Application

This project is a Java-based receipt generator that provides an easy way to store items, calculate totals, and generate printable receipts using a Graphical User Interface (GUI) built with Java Swing.

💡 Overview

The system is built using Object-Oriented Programming (OOP) principles and includes three main classes:

Item – Stores product details (ID, name, price).

Receipt – Manages added items, quantities, and total calculations.

ReceiptGUI – Provides a simple GUI for adding items and generating receipts.

⚙️ Features

Add products and quantities.

Auto-calculate total prices.

Display receipts in real-time.

Save receipts to a file.

Load item data from external files.

🧩 Tech Used

Language: Java

Framework: Swing GUI

Build Tool: Maven
